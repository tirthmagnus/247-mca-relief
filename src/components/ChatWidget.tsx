"use client";
import { useState } from "react";
import { MessageCircle,X,ArrowRight } from "lucide-react";
export default function ChatWidget(){const[open,setOpen]=useState(false);const[step,setStep]=useState(0);const[name,setName]=useState("");const[phone,setPhone]=useState("");const[consent,setConsent]=useState(false);const[done,setDone]=useState(false);async function send(){if(!name.trim()||!phone.trim()||!consent)return;const r=await fetch("/api/lead",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,phone,consent:true,source:"chat",url:location.href})});if(r.ok)setDone(true)}return <div className="chat"><button className="chatBtn" aria-label="Open chat" onClick={()=>setOpen(v=>!v)}>{open?<X/>:<MessageCircle/>}</button>{open&&<div className="chatPanel"><div className="chatTop"><strong>Quick business check-in</strong><span>Rule-based, no AI.</span></div>{done?<div className="chatDone"><strong>Thanks, {name}.</strong><p>Your request was received. A team member can follow up at {phone}.</p></div>:step===0?<div className="chatBody"><p>Are MCA withdrawals creating pressure on day-to-day operations?</p><button onClick={()=>setStep(1)}>Yes, I want to talk <ArrowRight/></button><button className="ghost" onClick={()=>setOpen(false)}>Not right now</button></div>:<div className="chatBody"><label>Name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Phone<input value={phone} onChange={e=>setPhone(e.target.value)} inputMode="tel"/></label><label className="miniConsent"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/>I agree to be contacted about this business inquiry.</label><button disabled={!name||!phone||!consent} onClick={send}>Send request</button></div>}</div>}</div>}
